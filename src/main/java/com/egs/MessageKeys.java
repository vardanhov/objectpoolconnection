package com.egs;

public interface MessageKeys {

    String MIN_SIZE = "Pool minimum size cannot be lower than 0";
    String MAX_SIZE = "Pool maximum size cannot be lower than 0";
    String MIN_GREATER_MAX = "Pool minimum size cannot be greater than maximum size";

}
